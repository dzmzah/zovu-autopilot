# Из чего мы делаем ролики и картинки — карта источников на 31.08.2026

Собрано под два вопроса, которые решают всё: **сколько это стоит** и
**можно ли отдавать клиенту** (коммерческая лицензия). Бесплатное без
коммерческих прав для клиентских работ не годится — там его место только
в пробах.

## Видео

| Чем | Сколько бесплатно | Лицензия | Как вызвать |
|---|---|---|---|
| **Veo 3.1 Lite по API** | платно: **0,05 $/с 720p** = 0,40 $ за 8 с | коммерческая | `wideo-veo.mjs`, ключ `GEMINI_API_KEY`, файл приходит сам |
| Veo 3.1 Fast / полный | 0,10 / 0,40 $ за секунду | коммерческая | тот же скрипт, `--model=` |
| **Google Flow (браузер)** | 50 бонусов в сутки = 5 клипов | коммерческая | руками; **файлы скачивает только Захар** |
| LTX на Hugging Face | бесплатно, очередь ZeroGPU | открытая | `wideo-hf.mjs`, нужен `HF_TOKEN` |
| Wan 2.2 (открытая модель) | бесплатно, своя RTX 5070 | Apache 2.0, коммерция ок | не подключено |
| Kling, Hailuo, PixVerse | 20–66 кредитов в сутки | **коммерции на бесплатном НЕТ** | только пробы |

**Почему API важнее бонусов.** Во Flow два потолка, и ни один не про
качество: пять клипов в сутки и невозможность скачать файл — песочница
расширения режет скачивание, поэтому клипы каждый раз вытаскивал Захар
руками. По API файл приходит на диск сам, модель та же (Veo 3.1 Lite),
вид не меняется.

**Стартовый кадр лечит «уплывшего» героя.** Текстом одну внешность между
клипами не удержать — в ролике Nootri мужчина в сценах «после» стал другим
человеком. С `obraz` в плане лицо приходит из файла, а не из описания.

## Картинки

| Чем | Сколько бесплатно | Лицензия | Как вызвать |
|---|---|---|---|
| **Pollinations** | без лимита и без ключа | свободная | уже в автопилоте |
| **Nano Banana Pro во Flow** | 0 бонусов за картинку | коммерческая | руками в браузере |
| Gemini image по API | платно, 0,039 $ за картинку | коммерческая | тот же `GEMINI_API_KEY` |
| Cloudflare Workers AI | 10 000 «нейронов» в сутки ≈ 2000 картинок 512×512 | условия не прописаны — проверить перед клиентом | нужен аккаунт Cloudflare |
| Ideogram | 10 в сутки, без водяного знака | **коммерческая** | нужен аккаунт; лучше всех держит текст в кадре |
| Живые фото Pexels | без лимита | свободная | уже в автопилоте |

## Голос

| Чем | Сколько бесплатно | Лицензия | Состояние |
|---|---|---|---|
| **Google Cloud TTS** | **1 млн знаков в месяц навсегда** (Chirp 3 HD), 4 млн Standard | коммерция разрешена | подключает соседняя сессия, ключ `GOOGLE_TTS_KEY` |
| Azure Speech | 500 тыс. знаков в месяц | коммерция разрешена | поддержка в `glos.mjs` уже написана, ключа нет |
| ElevenLabs, бесплатный | 10 тыс. знаков в месяц | **коммерции НЕТ**, требует упоминания сервиса | голоса из библиотеки по API закрыты (402) |
| ElevenLabs Starter 5 $ | 30 тыс. знаков | коммерческая | не покупали |
| Gemini TTS | ~15 запросов в сутки на бесплатном ключе | коммерческая | запасной путь, `GLOS_DOSTAWCA=gemini` |

## Что осталось на один клик Захара

**Привязать проект ключа `GEMINI_API_KEY` к платёжному аккаунту** (тому, где
лежат 300 $ триала). Сейчас ключ живёт на бесплатном уровне, а у Veo
бесплатного уровня НЕТ вообще — поэтому вызов возвращает 429 «exceeded your
current quota», хотя сам запрос правильный. Делается в AI Studio: ключ →
проект → включить биллинг.

После этого одним ключом открываются сразу три вещи: видео Veo по API,
картинки Gemini и суточные лимиты Gemini TTS перестают кончаться на второй
фразе.

## Крупные программы, которые стоит забрать отдельно

- **Microsoft for Startups Founders Hub** — до 150 000 $ кредитов Azure,
  без доли в фирме и без инвестора. Внутри — Azure Speech, то есть ещё и
  голос. Подаётся анкетой на фирму.
- **AWS** — до 200 $ новым аккаунтам на полгода.
- **NVIDIA Inception** — бесплатные эндпойнты моделей для прототипов.

Триал Google Cloud на 300 $ конечен: 90 дней. Поэтому платные вызовы Veo
держим для клиентских работ, а свою ленту продолжаем рисовать бесплатными
источниками.
